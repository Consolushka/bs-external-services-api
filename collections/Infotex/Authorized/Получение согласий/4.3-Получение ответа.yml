info:
  name: 4.3	Получение ответа
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/service/{{activePermissionsRequestUuid}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
