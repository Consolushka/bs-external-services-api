info:
  name: 5.3	Получение ответа
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/api/service/{{activeDataTypesRequestUuid}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
